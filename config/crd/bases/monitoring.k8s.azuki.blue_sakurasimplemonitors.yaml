---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: sakurasimplemonitors.monitoring.k8s.azuki.blue
spec:
  group: monitoring.k8s.azuki.blue
  names:
    kind: SakuraSimpleMonitor
    listKind: SakuraSimpleMonitorList
    plural: sakurasimplemonitors
    singular: sakurasimplemonitor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .status.health
      name: Health
      type: string
    - jsonPath: .status.monitorID
      name: MonitorID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SakuraSimpleMonitor is the Schema for the sakurasimplemonitors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SakuraSimpleMonitorSpec defines the desired state of SakuraSimpleMonitor.
            properties:
              description:
                description: Description is an optional operator-facing description.
                type: string
              healthCheck:
                description: HealthCheck defines how the controller should probe the
                  target.
                properties:
                  expectedStatus:
                    description: ExpectedStatus is the expected HTTP response code.
                    format: int32
                    maximum: 599
                    minimum: 100
                    type: integer
                  path:
                    description: Path is the HTTP request path.
                    minLength: 1
                    type: string
                  port:
                    description: Port is the destination TCP port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    description: Protocol is limited to HTTP-based checks in the initial
                      implementation.
                    enum:
                    - http
                    - https
                    type: string
                  timeoutSeconds:
                    description: TimeoutSeconds is the request timeout in seconds.
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - expectedStatus
                - path
                - port
                - protocol
                - timeoutSeconds
                type: object
              interval:
                description: Interval is the monitor interval in minutes.
                format: int32
                minimum: 1
                type: integer
              notifications:
                description: Notifications defines how alerts are sent.
                properties:
                  message:
                    description: Message is the fixed alert message body.
                    minLength: 1
                    type: string
                  repeatInterval:
                    description: RepeatInterval is the re-notification interval in
                      seconds.
                    format: int32
                    minimum: 1
                    type: integer
                  webhookURL:
                    description: WebhookURL is the destination webhook URL.
                    format: uri
                    minLength: 1
                    type: string
                required:
                - message
                - repeatInterval
                - webhookURL
                type: object
              retryInterval:
                description: RetryInterval is the retry interval in seconds.
                format: int32
                minimum: 1
                type: integer
              target:
                description: Target is the hostname or IP address to monitor.
                minLength: 1
                type: string
            required:
            - healthCheck
            - interval
            - notifications
            - retryInterval
            - target
            type: object
          status:
            description: SakuraSimpleMonitorStatus defines the observed state of SakuraSimpleMonitor.
            properties:
              conditions:
                description: Conditions describes the reconciliation state using Kubernetes-standard
                  conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              health:
                description: Health is the summarized monitor health reported by SakuraCloud.
                enum:
                - Unknown
                - Healthy
                - NotHealthy
                type: string
              lastSyncedAt:
                description: LastSyncedAt is the latest successful synchronization
                  timestamp.
                format: date-time
                type: string
              monitorID:
                description: MonitorID is the SakuraCloud simple monitor resource
                  ID.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest reconciled generation.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
